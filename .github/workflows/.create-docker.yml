name: Self hosted runner

on:
  push:
    branches: [ "AMQP" ]
  pull_request:
    branches: [ "AMQP" ]

jobs:

  build:
    runs-on: self-hosted
    environment: deploy-env

    steps:
      - name: Run Custom Script
        run: |
          echo "Hola mundo en el custom runner"
          echo "El valor de MI_VARIABLE es $SOMETHING_HERE"

